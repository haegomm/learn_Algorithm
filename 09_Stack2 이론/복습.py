# 부분집합

# def powerset(arr, start, total):
#
#     if total > 10:
#         return
#
#     if total == 10:
#         print(arr)
#         return
#
#     for i in range(start, len(numbers)):
#         arr.append(number[i])
#
#         powerset(arr, i + 1, total + number[i])
#
#         arr.pop()
#
#
# number = list(range(1, 11))
# powerset([], 0, 0)
